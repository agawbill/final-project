<div class="userWrapper">
  <div class="userLeft">

<h1>Public<span class="symbol">#</span>lists</h1>


<%@user.lists.each do |l|%>
<%if l.private == false%>


<p><div class="boxWrapper">

  <div class="userListWrapper">
<span class='movieTitle'> <a href="/lists/<%=l.id%>" class="listLink"><%=l.name%></a></span><br>
<span class="blogDateTime"><span class="postedBy">Created </span> <%=l.created_at.strftime(
'%a, %b %Y'
)%> <span class="postedBy">at</span> <%=l.created_at.strftime(
'%I:%M %p'
)%> ET</span>
<span class="listCount"><%=l.movie_ids[0].count%> movies</span></div>
<p><%=l.description%>
</div></p>



<%end%>
<%end%>
</div>

<div class="userRight">
  <h1>Private<span class="symbol">#</span>lists</h1>

  <%@user.lists.each do |l|%>
  <%if l.private == true%>


  <p><div class="boxWrapper">

    <div class="userListWrapper">
  <span class='movieTitle'> <a href="/lists/<%=l.id%>" class="listLink"><%=l.name%></a></span><br>
  <span class="blogDateTime"><span class="postedBy">Created </span> <%=l.created_at.strftime(
  '%a, %b %Y'
  )%> <span class="postedBy">at</span> <%=l.created_at.strftime(
  '%I:%M %p'
  )%> ET</span>
  <span class="listCount"><%=l.movie_ids[0].count%> movies</span></div>
  <p><%=l.description%>
  </div></p>



  <%end%>
  <%end%>


</div></p>
</div>
<script>
</script>
